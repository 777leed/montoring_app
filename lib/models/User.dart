class User {
  final String uid; // Unique user ID
  final String email; // User's email address
  final String name; // User's name

  User({
    required this.uid,
    required this.email,
    required this.name,
  });
}
